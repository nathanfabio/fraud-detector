services:
  lb:
    image: nathanfabio/fraud-detector4:v37
    command: ["./lb", "9999", "/run/sock/api1.sock", "/run/sock/api2.sock"]
    ports:
      - "9999:9999"
    volumes:
      - sock-vol:/run/sock:rw
    depends_on:
      - api1
      - api2
    networks:
      - app-net
    mem_limit: 8m
    cpus: "0.05"

  api1:
    image: nathanfabio/fraud-detector4:v37
    command: ["./server"]
    hostname: api1
    volumes:
      - sock-vol:/run/sock:rw
    networks:
      - app-net
    mem_limit: 170m
    cpus: "0.475"

  api2:
    image: nathanfabio/fraud-detector4:v37
    command: ["./server"]
    hostname: api2
    volumes:
      - sock-vol:/run/sock:rw
    networks:
      - app-net
    mem_limit: 170m
    cpus: "0.475"

volumes:
  sock-vol:

networks:
  app-net:
    driver: bridge
